{% extends 'base.html.twig' %}

{% block title %}Edit Price StockItem{% endblock %}

{% block body %}
    <h1>Edit Price StockItem</h1>

    <p>Prix d'achat {{ stock_item.finalPayPrice }} €</p>
    <p>Prix conseillé {{ stock_item.finalSellPrice }} €</p>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_stock_item_index') }}">back to list</a>

    {# {{ include('stock_item/_delete_form.html.twig') }} #}
{% endblock %}
