{% extends 'base.html.twig' %}

{% block title %}WorldData{% endblock %}

{% block body %}
    <h1>WorldData</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ world_datum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ world_datum.name }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ world_datum.number }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ world_datum.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_world_data_index') }}">back to list</a>

    <a href="{{ path('app_world_data_edit', {'id': world_datum.id}) }}">edit</a>

    {{ include('world_data/_delete_form.html.twig') }}
{% endblock %}
